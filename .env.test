# Test Environment Configuration
# Used during pytest execution

# Application settings
SECRET_KEY=test-secret-key-for-testing-only
DEBUG=true
TESTING=true
PROJECT_NAME=AgriTech Platform (Test)

# Database (SQLite for testing)
DATABASE_URL=sqlite+aiosqlite:///./test.db
DB_HOST=localhost
DB_USER=test_user
DB_PASSWORD=test_password
DB_NAME=agritech_test

# Redis (Mock for testing)
REDIS_URL=redis://localhost:6379/1
REDIS_PASSWORD=test_redis_password

# Email (Mock for testing)
MAIL_USERNAME=test@example.com
MAIL_PASSWORD=test_password
MAIL_FROM=test@agritech.com
MAIL_PORT=587
MAIL_SERVER=smtp.example.com
MAIL_TLS=true
MAIL_SSL=false

# External APIs (Mock endpoints)
CLOUDINARY_CLOUD_NAME=test_cloud
CLOUDINARY_API_KEY=test_api_key
CLOUDINARY_API_SECRET=test_api_secret

# ML/AI (Disabled for testing)
ML_ENABLED=false
MODEL_TRAINING_ENABLED=false

# Security (Relaxed for testing)
JWT_SECRET_KEY=test-jwt-secret-key
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# SMS (Mock for testing)
SMS_ENABLED=false
TWILIO_ACCOUNT_SID=test_account_sid
TWILIO_AUTH_TOKEN=test_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Push Notifications (Mock for testing)
FCM_SERVER_KEY=test_fcm_server_key
APNS_KEY_ID=test_apns_key_id
APNS_TEAM_ID=test_apns_team_id

# CORS
CORS_ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:8000"]

# Rate Limiting (Relaxed for testing)
RATE_LIMIT_ENABLED=false
RATE_LIMIT_CALLS=1000
RATE_LIMIT_PERIOD=60

# Logging
LOG_LEVEL=INFO
